import React from "react";
import AuthListener from "../components/AuthListener";

const Post = () => {
  return (
    <div>
      <AuthListener />
      All posts here
    </div>
  );
};

export default Post;
